const input = require('readline-sync')

let impar = 0
let par = 0
let num = 0


for (let i = 0; i <= 10; i++)
    numeros = Number(input.question(`escreva 10 números: `))

if(numeros % 2 === 0){

    pares
}

